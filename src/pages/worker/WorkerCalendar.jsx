import React from 'react'
import CalendarGrid from '../../components/calendar/CalendarGrid'

const WorkerCalendar = () => (
  <div>
    <h2>Mi Calendario (solo lectura)</h2>
    <CalendarGrid />
  </div>
)

export default WorkerCalendar
